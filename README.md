# CGT-215
This repository contains my CGT 215 lab assignments written in C++ using Visual Studio 2022.

## Lab 02
This lab uses:
- `cin` for user input
- `cout` for output
- `if` / `else if` statements
- Functions
- Basic mathematical operations

## Lab 03

This lab focuses on using functions to organize a C++ program.

This lab uses:
- separate functions for user input
- separate functions for mathematical operations
- a function to display the result
- Keeps `cin` and `cout` outside of `main()`
- `if` / `else if` statements to determine which operation to perform
